# 执行模式：建议先用 trace，本地验证通过后再切到 live
EXECUTION_MODE=trace

# 通用必填：被跟单的钱包与 MongoDB
USER_ADDRESS=0xYourTargetWalletAddress
MONGO_URI=mongodb://localhost:27017/polymarket_copytrading

# 通用建议参数
FETCH_INTERVAL=1
SETTLEMENT_SWEEP_INTERVAL_MS=3000
# 冷启动时向前回看多少秒的活动
INITIAL_SYNC_LOOKBACK_SECOND=24
RETRY_LIMIT=3

# 日志：默认控制台+文件双输出；文件路径默认 logs/bot.log
LOG_LEVEL=info
LOG_CONSOLE_LEVEL=info
LOG_FILE_LEVEL=debug
LOG_CONSOLE_ENABLED=true
LOG_FILE_ENABLED=true
LOG_FILE_PATH=logs/bot.log

# trace 模式：本地模拟，不发真实订单
TRACE_ID=default
TRACE_INITIAL_BALANCE=1000
TRACE_SOURCE_LOOKBACK_MS=0

# live 模式：真实下单时必须填写
PROXY_WALLET=0xYourProxyWalletAddress
# 必须是 64 位十六进制字符串，且不要带 0x 前缀
PRIVATE_KEY=abc678def4567890abcdef1234567890abcdef1234567890abcdef1234567890
MAX_SLIPPAGE_BPS=300
MAX_ORDER_USDC=0
PROCESSING_LEASE_MS=30000
AUTO_REDEEM_ENABLED=true
MARKET_WS_ENABLED=true
ACTIVITY_ADJACENT_MERGE_WINDOW_MS=15000
BUY_SIZING_MODE=signal_fixed_ticket
SIGNAL_BUFFER_MS=5000
SIGNAL_IGNORE_BUY_BELOW_USDC=0.25
SIGNAL_WEAK_SOURCE_BUY_USDC=8
SIGNAL_WEAK_SOURCE_BUY_COUNT=2
SIGNAL_SINGLE_TRADE_WEAK_USDC=8
SIGNAL_MIN_SOURCE_BUY_USDC=12
SIGNAL_MIN_SOURCE_BUY_COUNT=3
SIGNAL_STRONG_SOURCE_BUY_USDC=20
SIGNAL_STRONG_SOURCE_BUY_COUNT=5
FOLLOW_WEAK_TICKET_USDC=1.0
FOLLOW_FIXED_TICKET_USDC=1.5
FOLLOW_STRONG_TICKET_USDC=2.0
FOLLOW_MAX_OPEN_POSITIONS=8
FOLLOW_MAX_ACTIVE_EXPOSURE_USDC=18
FOLLOW_MARKET_SCOPE=crypto_updown_5m
FOLLOW_MAX_TICKETS_PER_CONDITION=2
FOLLOW_POSITION_DUST_USDC=0.5

# 仅在 BUY_SIZING_MODE=first_entry_ticket 时生效
BUY_FIRST_ENTRY_TICKET_USDC=1
BUY_FIRST_ENTRY_SIGNAL_MIN_USDC=0.05

# 仅在 ratio / first_entry_ticket 模式下参与碎单补齐
BUY_MIN_TOP_UP_ENABLED=true
BUY_MIN_TOP_UP_TRIGGER_USDC=0.7
BUY_BOOTSTRAP_MAX_ACTIVE_RATIO=0.15
BUY_INTENT_BUFFER_MAX_MS=2000
BUY_DUST_RESIDUAL_MODE=trim
LIVE_STATE_REFRESH_MS=1000
LIVE_MAX_STALE_SNAPSHOT_MS=5000
LIVE_EXECUTOR_LOOP_INTERVAL_MS=100
LIVE_CONFIRM_TIMEOUT_MS=45000
LIVE_RECONCILE_AFTER_TIMEOUT_MS=3000
LIVE_PERSIST_MAX_QUEUE_SIZE=2000
LIVE_PERSIST_RETRY_MS=1000

# 可选：仅当需要覆盖官方默认基础设施时再取消注释
# CLOB_HTTP_URL=https://clob.polymarket.com
# POLYMARKET_WS_BASE_URL=wss://ws-subscriptions-clob.polymarket.com/ws
# RPC_URL=https://polygon.drpc.org
# USDC_CONTRACT_ADDRESS=0x2791Bca1f2de4661ED88A30C99A7a9449Aa84174
# POLYMARKET_RELAYER_URL=https://relayer-v2.polymarket.com
# POLYMARKET_RELAYER_TX_TYPE=PROXY
# POLYMARKET_CTF_CONTRACT_ADDRESS=0x4d97dcd97ec945f40cf65f87097ace5ea0476045

# 可选：高级同步与确认参数
# ACTIVITY_SYNC_LIMIT=100
# ACTIVITY_SYNC_OVERLAP_MS=30000
# SNAPSHOT_STALE_AFTER_MS=30000
# ORDER_CONFIRMATION_TIMEOUT_MS=45000
# ORDER_CONFIRMATION_POLL_MS=2000
# ORDER_CONFIRMATION_BLOCKS=2

# 可选：共享 WebSocket 参数；如需分开覆盖，再改用通道级变量
# POLYMARKET_WS_RECONNECT_MS=1000
# MARKET_CACHE_TTL_MS=3000
# MARKET_WS_SNAPSHOT_WAIT_MS=750

# 可选：自动回收节流与 builder 鉴权
# 未显式设置 builder 凭据时，实盘会尝试通过当前 CLOB L2 身份自动创建进程内 builder key
# AUTO_REDEEM_INTERVAL_MS=30000
# AUTO_REDEEM_MAX_CONDITIONS_PER_RUN=8
# POLY_BUILDER_API_KEY=
# POLY_BUILDER_SECRET=
# POLY_BUILDER_PASSPHRASE=

# 兼容旧配置：通道级变量仍可单独覆盖共享值
# CLOB_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/market
# USER_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/user
# MARKET_WS_RECONNECT_MS=1000
# USER_WS_RECONNECT_MS=1000
